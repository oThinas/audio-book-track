---
name: Use schemas folder not validations
description: User prefers lib/schemas/ over lib/validations/ for Zod schema files
type: feedback
---

Use `lib/schemas/` instead of `lib/validations/` for Zod schema files.

**Why:** User preference for naming convention.

**How to apply:** When creating Zod schemas, place them in `src/lib/schemas/` directory.